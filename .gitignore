# Secrets
.env
.env.*
!.env.example

# Python
.venv/
venv/
.pydeps/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.ipynb_checkpoints/

# Data — track small artifacts (weekly_pull txt, cftc zips); ignore the big rebuildable
# SQLite store + WAL sidecars + local SQL backups (all under data/database/).
data/database/
# Runtime logs + replay-derived CSV — churn every run, rebuilt from source / the DB.
data/logs/
data/trade_outcomes.csv
# Provider OHLC caches + quarantine logs — DB is source of truth; CSVs are throwaway pull caches.
data/twelvedata/
*_quarantine.csv

# Claude local — ignore settings but SHARE commands/
.claude/*
!.claude/commands/
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

/wiki/research-papers
